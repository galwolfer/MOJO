/**
 * CalendarEventWidget — DEPRECATED
 *
 * This widget has been removed from the UI and LLM registry per project change.
 * The file is retained as a stub to avoid accidental import-time errors.
 */

import React from "react";

const CalendarEventWidget = () => {
  // Intentionally render nothing — the widget is removed
  return null;
};

export default CalendarEventWidget;
